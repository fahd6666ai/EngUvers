// EngUvers doesn't add an extra i18next namespace through this seam —
// same no-op as the OSS default. The Circuit Lab page's own Arabic UI
// lives entirely in apps/web, outside this embedded simulator.
export function registerProI18n(): void {}
