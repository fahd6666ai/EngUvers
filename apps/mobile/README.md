# apps/mobile — EngUvers mobile app

**Status: placeholder — implemented in Phase 8.**

Planned: Expo / React Native app reusing `apps/api`'s HTTP API and
`packages/types`' shared domain types, with the Circuit Lab embedded via
WebView (same Bridge `postMessage` contract as the web iframe integration
in `services/simulator`).

## Why this is empty right now

Phase 0 scaffolds only what Phase 1 needs (`apps/web`, `apps/api`). This
directory exists so the target monorepo shape is visible from the start;
it is scaffolded for real at the start of Phase 8.
